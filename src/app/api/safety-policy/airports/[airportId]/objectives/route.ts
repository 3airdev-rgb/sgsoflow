import { NextResponse } from "next/server"; import { getAuthorizationContext, requireSession } from "@/server/auth/session"; import { errorResponse } from "@/server/errors"; import { assertNotLocalPreviewMutation } from "@/server/local-preview"; import { createSafetyObjective } from "@/server/safety-policy/service"; import { safetyObjectiveSchema } from "@/server/validation/schemas";
export async function POST(request: Request, { params }: { params: Promise<{ airportId: string }> }) { try { const session = await requireSession(); assertNotLocalPreviewMutation(session); const { airportId } = await params; const { organizationId, ...input } = safetyObjectiveSchema.parse(await request.json()); const authz = await getAuthorizationContext(session.userId); return NextResponse.json({ objective: await createSafetyObjective({ actor: session, authz, organizationId, airportId, userId: session.userId }, input) }, { status: 201 }); } catch (error) { return errorResponse(error); } }
