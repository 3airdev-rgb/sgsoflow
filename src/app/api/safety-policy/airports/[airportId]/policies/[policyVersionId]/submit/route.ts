import { NextResponse } from "next/server"; import { getAuthorizationContext, requireSession } from "@/server/auth/session"; import { errorResponse } from "@/server/errors"; import { assertNotLocalPreviewMutation } from "@/server/local-preview"; import { submitSafetyPolicyForReview } from "@/server/safety-policy/service"; import { safetyPolicyScopeSchema } from "@/server/validation/schemas";
export async function POST(request: Request, { params }: { params: Promise<{ airportId: string; policyVersionId: string }> }) { try { const session = await requireSession(); assertNotLocalPreviewMutation(session); const { airportId, policyVersionId } = await params; const { organizationId } = safetyPolicyScopeSchema.parse(await request.json()); const authz = await getAuthorizationContext(session.userId); return NextResponse.json({ version: await submitSafetyPolicyForReview({ actor: session, authz, organizationId, airportId, userId: session.userId }, policyVersionId) }); } catch (error) { return errorResponse(error); } }
